this is a file where only this line is important.

/* All of this is MY CODE AND YOU CAN'T HAVE IT */
Well... ok, just the first time.

/* This is JDS's code!! I want this stuff on my local file, but not to share*/
	printCount1++;
        if (printCount1 >= 60000) //every .5ish seconds
        {
            DOUT_LED1_Toggle();
            printCount1 = 0;
            printCount2++;
            if (printCount2 >= 10) //every 5ish seconds
            {
                printCount2 = 0; 
                printCount3++;
                printf("\r\n");
                printf("%u\r\n",printCount3);
                printf("Precharge relay state: %u\r\n", _LATD13);
                printf("Doubler relay state: %u\r\n", _LATC0);

                printf("ReadVBUS() %u\r\n", ReadVBUS());
                printf("ReadIGBTTemp() %u\r\n", ReadIGBTTemp());
                ReadAbsolutePosition(&AbsolutePosition);

                printf("ReadVBUS() %u\r\n", VoltageBusIn);
                printf("ReadIGBTTemp() %u\r\n", ReadIGBTTemp());ReadAbsolutePosition(&AbsolutePosition);

                printf("POS1CNT: %u \r\n", POS1CNTL);
            }
        }

